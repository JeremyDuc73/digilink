{% extends 'base.html.twig' %}

{% block title %}Edit Link{% endblock %}

{% block body %}
    <div class="container-lg">
        <h1>Edit Grade</h1>

        {{ include('grade/_form.html.twig', {'button_label': 'Update'}) }}

        <a class="btn btn-secondary fs-6 fw-light" href="{{ path('app_grade_index') }}">back to list</a>

    </div>
{% endblock %}