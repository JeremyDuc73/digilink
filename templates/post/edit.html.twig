{% extends 'base.html.twig' %}

{% block title %}Site trop cool{% endblock %}

{% block body %}
    <h1>{{ title }}</h1>

    {{  form_start(postForm) }}

    {{  form_row(postForm.title) }}
    {{  form_row(postForm.content) }}


    <button class="btn btn-primary" type="submit">{{ button }}</button>
    {{  form_end(postForm) }}



{% endblock %}